# Mod Properties
mod.id=villagercycle
mod.name=VillagerCycle
mod.version=1.5.0

# Gradle JVM Settings
org.gradle.jvmargs=-Xmx2G -XX:MaxMetaspaceSize=512m

# Shared Dependencies (used across all versions)
deps.fabric_loader=0.18.4
